### WebStorm .editorconfig 설정
```angular2html
settings > Editor > Code Style > JavaScript/TypeScript >
Set from > Editor Config 클릭
```

```angular2html
npm install @radix-ui/themes --save

npm i tailwind-merge --save

```
